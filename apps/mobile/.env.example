# Choose an API host reachable from the Android/iOS simulator or physical device.
# This Expo public value must contain only the API base URL; never put a session token or backend secret here.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
